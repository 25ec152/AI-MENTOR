"""
Voice Assistant handlers package.

Each module in this package exposes one or more handler functions that accept
a payload string and return a human-readable reply string.

Modules (added in later sub-tasks):
  app_launcher   — open desktop applications (Chrome, VS Code, …)
  web_opener     — open websites / Google search
  system_info    — time, date, battery percentage
  system_control — lock, restart, shutdown
"""
